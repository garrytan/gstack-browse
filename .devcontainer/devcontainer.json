{
  "name": "Node.js & TypeScript",
  "build": {
    "dockerfile": "Dockerfile",
    "context": "."
  },
  "customizations": {
    "vscode": {
      "extensions": ["oven.bun-vscode"]
    }
  },
  "postCreateCommand": "bun install --frozen-lockfile && bash ./devcontainer/link-skills.sh",
  "remoteUser": "node"
}
