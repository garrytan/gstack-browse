{
  "name": "research-stack",
  "version": "0.1.0.0",
  "description": "Research computation skills for Claude Code — hypothesis, experiment, report workflow with convention enforcement and provenance tracking.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "bun run gen:skill-docs",
    "gen:skill-docs": "bun run scripts/gen-skill-docs.ts",
    "test": "bun test test/skill-validation.test.ts",
    "skill:check": "bun run scripts/skill-check.ts",
    "dev:skill": "bun run scripts/dev-skill.ts"
  },
  "dependencies": {
    "diff": "^7.0.0"
  },
  "engines": {
    "bun": ">=1.0.0"
  },
  "keywords": [
    "research",
    "computation",
    "numerical-simulation",
    "claude",
    "ai-agent",
    "reproducibility"
  ],
  "devDependencies": {
    "@anthropic-ai/sdk": "^0.78.0"
  }
}
